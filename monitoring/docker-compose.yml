version: "3.9"

services:
  grafana:
    build: .
    container_name: grafana
    restart: on-failure
    ports:
      - "3000:3000"
    networks:
      - nyx-network

networks:
  nyx-network:
    name: nyx-network
    driver: bridge
